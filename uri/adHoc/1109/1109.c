#include <stdio.h>
#include <stdlib.h>

enum
{
  FALSE,
  TRUE
};

static int char1(char value)
{
  
}

int main(int argc, char ** argv)
{
  char regex[160], word[100];
  int testCase = 0;

  while(scanf("%s", regex) != EOF)
  {
    scanf("%d", &testCase);
    while(testCase--)
    {
      scanf("%s", word);
    
    }
  }

  return 0;
}
